.col-sm-4.col-md-3.proto-content
  - if prototype.user == current_user
    = render partial: 'prototype_action', locals: {prototype: prototype}
  .thumbnail
    = link_to image_tag(prototype.images.main.first.name.url), prototype_path(prototype.id)
    .caption
      %h3
        = prototype.title
      .proto-meta
        .proto-user
          %a{:href => "#"} by #{prototype.user.username}
        .proto-posted
          = prototype.created_at.strftime("%b %d")
      -# %ul.proto-tag-list.list-inline
      -#   %li
      -#     %a{:href => "#", :class => "btn btn-default"} iPad
      -#   %li
      -#     %a{:href => "#", :class => "btn btn-default"} wireframe